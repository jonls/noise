/* misc.h*/

#ifndef _MISC_H
#define _MISC_H

#define max(x,y)  (((x) > (y)) ? (x) : (y))
#define min(x,y)  (((x) < (y)) ? (x) : (y))

#define FASTFLOOR(x)  (((x) >= 0) ? (int)(x) : (int)(x)-1)

#endif /* !_MISC_H */
